# 이전 상담·수강생 데이터 분석 및 활용 계획

분석 원본: `트라이앵글 이전 상담데이터.txt` (원본은 프로젝트로 복사하거나 수정하지 않음)

## 1. 데이터 현황

- 3~5기 상담 기록: 26건
  - 3기 10건
  - 4기 8건
  - 5기 8건
- 3~7기 수강생 프로필 메모: 17건
- 3~5기 상담자 대비 수강생 목록의 단순 비율: 14 / 26 = 53.8%
  - 목록 누락과 동명이인 가능성이 있으므로 확정 전환율이 아니라 초기 참고값으로만 사용한다.
- 수강생 프로필 17건의 평균 연령: 약 29.2세
- 수강생 경험 분포
  - 연기 전공: 8명 (47.1%)
  - 연기 처음: 3명 (17.6%)
  - 연기학원 경험: 3명 (17.6%)
  - 미기재: 3명 (17.6%)
- 성별이 기재된 수강생은 15명이며 여성 10명, 남성 5명이다. 2명은 미기재다.
- 개강 뒤 추가 모집 기간에 결제한 것으로 표시된 상담 기록이 2건 있다.

## 2. 정합성 위험

- 연도 없이 월·일만 있는 기록이 많아 기수 개강일을 기준으로 연도를 추정해야 한다.
- 동일 이름이 다른 기수·다른 연령으로 등장하는 사례가 있어 이름만으로 자동 병합하면 안 된다.
- 일부 연령·성별·상담 결과가 비어 있다.
- 상담 목록과 수강생 목록이 별도 구역에 있어 실제 전환 여부는 후보 매칭 후 관리자 확인이 필요하다.
- 원문 메모에 자유 형식 표현이 섞여 있어 정규화 값과 원문을 함께 보존해야 한다.

## 3. 권장 저장 구조

### 운영 원장: Supabase PostgreSQL

- `consultation_sessions`: 상담일, 기수 후보, 상담 대상, 전환 상태
- `consultation_transcripts`: 상담 원문 또는 기존 메모
- `consultation_analyses`: Gemini 분석 결과와 분석 버전
- `consultation_action_items`: 후속조치
- `actors` / `inquiries`: 관리자 확인 후 연결되는 실제 인물 레코드

### 원본 파일: Supabase Storage 또는 Google Drive

- Supabase Storage private bucket: 앱과 직접 연결되는 원본 TXT, 향후 녹음 파일
- Google Shared Drive: 사람이 열람하는 장기 자료, 운영 문서, 월별 CSV/PDF 백업
- Google Drive를 CRM 원장으로 사용하지 않는다. 검색·중복 방지·관계형 분석은 PostgreSQL이 담당한다.

## 4. 이전 데이터 가져오기 방식

1. 원본 파일 해시와 가져오기 일시를 기록한다.
2. 각 줄을 staging row로 분리한다.
3. 날짜, 기수, 연령, 성별, 경험, 결제 메모를 정규화한다.
4. 이름·기수·연령을 이용해 기존 배우/문의와 매칭 후보를 만든다.
5. 관리자 화면에서 `신규`, `기존 인물 연결`, `동명이인`, `보류`를 선택한다.
6. 확인된 행만 운영 테이블에 승격한다.
7. 승격 후 기수별 상담수, 수강 전환율, 경험 분포, 추가 모집 성과를 집계한다.

## 5. 우선 활용 지표

- 기수별 상담 수와 수강 전환율
- 상담일에서 결제·개강까지 걸린 기간
- 연기 전공/초보/학원 경험별 전환율
- 정규 모집과 추가 모집의 전환 차이
- 상담 후 미전환자 재접촉 대상
- 반복 문의와 상담 스크립트 개선 후보

## 6. 다음 구현 우선순위

1. `legacy_import_batches`, `legacy_import_rows` staging 테이블 설계
2. 이전 TXT 미리보기·행 파싱·오류 표시 화면
3. 기존 배우/문의 후보 매칭과 관리자 확정 화면
4. 확정 행의 상담/수강생 데이터 승격
5. 기수별 전환 대시보드
6. private Storage 원본 보관과 정기 외부 백업
