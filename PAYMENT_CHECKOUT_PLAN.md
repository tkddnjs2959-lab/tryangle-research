# 수강생 웹 결제 연동 구상

## 권장 1차 방식

국내 수강생 중심이면 Toss Payments 같은 국내 PG를 1차 후보로 두고, 결제 데이터의 기준 원장은 Supabase로 유지한다. 특정 PG SDK에 화면과 데이터 모델을 종속시키지 않고 `checkout_orders`를 중간 계층으로 둔다.

## 결제 흐름

1. 수강생 로그인 후 자신의 수강 과정과 금액을 확인한다.
2. 서버가 `checkout_orders`에 `ready` 주문을 만든다. 금액은 클라이언트가 보낸 값을 신뢰하지 않고 서버의 상품·등록 정보에서 계산한다.
3. 결제창으로 이동한다. `order_id`와 금액을 PG에 전달한다.
4. 결제 성공 후 서버가 PG 승인 API를 호출한다. 성공 리다이렉트만으로 결제완료 처리하지 않는다.
5. 승인 결과의 금액·주문번호·결제키를 검증하고 `checkout_orders.status = paid`로 갱신한다.
6. 같은 트랜잭션 흐름에서 `payments`에 결제 원장을 추가하고 `enrollments.status = enrolled`로 변경한다.
7. 웹훅은 중복 호출을 전제로 멱등 처리한다. 환불·부분환불은 별도 원장 이벤트로 남긴다.

## 화면 구성

- `/checkout`: 상품·금액·환불 정책·개인정보 동의
- `/checkout/success`: 서버 승인 결과만 표시
- `/checkout/fail`: 실패 사유와 재시도
- 관리자 `/admin/funnel`: 등록별 주문·결제 상태, 환불 메모, 수동 보정

## 보안 기준

- PG secret key와 Supabase service role은 서버 환경변수에만 둔다.
- 결제 금액은 서버에서 재계산한다.
- 성공 URL을 결제완료 근거로 사용하지 않는다.
- `provider_payment_key`, 원 응답은 관리자 서버 전용 테이블에만 저장한다.
- 주문번호는 매 요청마다 유일하게 생성하고 웹훅은 `provider_payment_key` 기준으로 중복 방지한다.

## 마케팅 분석 연결

결제 완료 시 `checkout_orders → payments → enrollments → inquiry`를 따라 최초 유입 `source / medium / campaign`을 보존한다. 이후 `marketing_spend_daily`의 광고비와 결합해 다음 지표를 계산한다.

- CPL = 광고비 / 문의 수
- 등록 CPA = 광고비 / 확정 등록 수
- ROAS = 결제완료 매출 / 광고비

## 현재 광고 기준값

- 현재 운영 기준: 인스타그램 일 20,000원
- 월 환산 기준: 600,000원
- 과거 기수: 일 20,000원 수준, 실제 집행일과 캠페인별 금액은 광고 관리자 내보내기 파일로 별도 입력

실제 PG 키, 사업자·정산 정보, 상품 금액과 환불 정책이 확정되면 PG별 결제창과 승인·웹훅 API를 연결한다.

현재 관리자 등록 화면에서 상품명과 수강료를 먼저 저장할 수 있게 연결했다. PG 연동 전에는 금액이 없는 등록을 결제 대상으로 만들지 않는다.
